# GTD 清单整理模式

从 2026-07-14 日志蒸馏为 GTD 清单的过程中观察到的用户偏好。

## 板块结构（按时间敏感度降序）

```
日程 📅          → 有时间约束的事件
下一步行动        → 当下可以动手的事
等待回复          → 等别人回应的
悬而未决          → 暂不明确怎么推进的
```

- 空的板块不保留占位，直接删除。
- 已完成事项不留"完成"板块归档。

## 归类原则

- **与日程强关联的事 → 合并进日程条目**，不另设独立行动项。
  例：顾问提案的"考虑落地可行性"没有单独列在"下一步行动"，而是作为周四会议的子说明。

## 优先级标注

- 不使用 emoji/符号，统一用 `> 优先级：低/中` 文本标注。

## 事项迁移

随着对事项状态判断的变化，在板块间动态移动：

| 状态变化 | 迁移方向 |
|---|---|
| 需要等别人回应 | → 等待回复 |
| 与会议绑定，会上处理 | → 日程（从下一步行动移入） |
| 暂时不确定怎么推进 | → 悬而未决 |

## 提交流程

- 每个修改独立提交，不批量攒着。
- 子模组 `data/profile` 先 commit，父仓库再 commit 更新子模组引用。
